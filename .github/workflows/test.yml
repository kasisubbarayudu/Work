on:
  push:
    branches: master

jobs:
  job1:
    runs-on: ubuntu-latest
    steps:
    - name: checking the pre-requisites...
    uses: kasisubbarayudu/Work/.github/workflows/main.yml@master
    - name: Test with pytest
      run: |
        pytest -vv

